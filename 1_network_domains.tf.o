module "network_domains" {
  source  = "terraform-aviatrix-modules/mc-network-domains/aviatrix"
  version = "v1.0.0"

  connection_policies = [
    ["non_pci", "shared"],
    ["pci", "shared"]
  ]

  additional_domains = [
    "non_pci",
    "pci",
    "shared"
  ]
}
